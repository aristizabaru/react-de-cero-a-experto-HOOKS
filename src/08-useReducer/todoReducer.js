export const todoReducer = (initialState = [], action) => {
    switch (action.type) {
        case 'ABC':

            return initialState;

        default:
            return initialState;
    }
};